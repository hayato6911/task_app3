<!DOCTYPE html>
<html>
  <head>
    <title>TaskApp3</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    
<header class="header">
  <div class="header-left-nav">
        <a href="/"><img alt="サイトのロゴ画像" src="https://presite-potepancamp-rails-02.herokuapp.com/assets/logo.png" /></a>
    <%= search_form_for @q  do |f| %>
      <%= f.search_field :address_cont, placeholder: 'エリア検索' %>
      <%= f.search_field :room_name_cont, placeholder: 'キーワード検索' %>
      <%= f.submit "検索" %>
    <% end %>
  </div>
  <div class="header-right-nav">
        <% if user_signed_in? %>
      <div class="nav-item d-sm-inline-block d-none">ユーザー名：<%= current_user.username %></div>
      <div class="menu">
      <div class="menu-title">メニュー</div>
      <div class="sub-menu">
        <ul>
          <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "button is-warning is-light" %></li>
          <li><%= link_to "マイページ", new_user_session_path, class: "button is-warning is-light" %></li>
          <li class="room"><%= link_to "ルーム登録", :new_room %></li>
          <li><%= link_to '登録済みルーム一覧', rooms_posts_path %></li>
          <li><%= link_to '予約済みルーム一覧', resevations_posts_path %></li>
        </ul>
      </div>
    </div>
    <% else %>
     <%= link_to "ログイン", new_user_session_path, class: "button is-warning is-light" %>
     <%= link_to "新規登録", new_user_registration_path, class: "button is-warning is-light" %>
    <% end %>
  </div>
</header>
    
    <% if flash[:notice] %>
      <div class="notification is-info">
        <p class="notice"><%= notice %></p>
      </div>
    <% end %>
    
    <% if flash[:alert] %>
      <div class="notification is-danger">
        <p class="alert"><%= alert %></p>
      </div>
    <% end %>
    
    
    <%= yield %>
    
    <footer class="footer">
      <p>© 2020 POTEPAN.INC.</p>
    </footer>
  </body>
</html>